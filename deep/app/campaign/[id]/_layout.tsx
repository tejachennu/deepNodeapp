import { Stack } from 'expo-router';

export default function CampaignIdLayout() {
    return (
        <Stack
            screenOptions={{
                headerBackTitle: 'Back',
            }}
        />
    );
}
